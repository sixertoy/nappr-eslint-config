import { nodeConfig } from '../configs/index.config.js';

/**
 * Configuration recommandée pour projets Node.js
 * Configuration complète prête à l'emploi avec support des tests Vitest
 */
export const node = nodeConfig;
