import { reactConfig } from '../configs/index.config.js';

/**
 * Configuration recommandée pour projets React
 * Configuration complète prête à l'emploi avec toutes les règles
 */
export const react = reactConfig;
